1.创建本地仓库：
    1）创建新文件夹
    2）git init
2.把本地内容推送到自己的远程仓库
    1）建立远程仓库
    2）git remote add 远程通道名 远程通道地址
    3）git工作流：
        1.git add .
        2.git commit -m""
        3.git push origin master
3.
    1)登录自己的github
    2)把讲义地址复制到地址栏-enter
    3）fork
    4）拷贝自己远程仓库的地址，通过git clone 地址---把他拷贝到本地
4.删除远程仓库: git remote rm origin(通道名称)
5.内存：栈内存 和 堆内存
栈内存：全局作用域 和 私有作用域
    全局作用域：是在浏览器加载完页面的时候形成，所以，关闭浏览器就可以释放全局作用域；
    私有作用域：一般情况下，等函数执行完成的时候，私有作用域会自动销毁；
        私有作用域不会销毁的情况：
        1.不销毁：但函数中有东西被外面占用的时候，就不会销毁；
        2.不立即销毁：当函数执行完成会返回一个需要再次被执行的函数，等里面这个小函数执行完成，所有的函数才能销毁；
堆内存：存放数据的；var fn=xxff00; var a=xxff11;
堆内存一般都是被变量或者函数名占用，所以不能释放，如果想释放，我们需要把函数名或者变量名指向空指针，这样，等浏览器空闲的时候，会把没有被占用的内存都回收；这种就是浏览器的垃圾回收机制；
fn=null; a=null;
6.this的总结：
1）元素身上的行为/事件被触发的时候，会执行一个函数，函数中的this指向当前这个元素
2）函数被调用的，看前面是否有点，点前面是谁，this就是谁
3）自执行函数中的this，都是window；
7.闭包：
1）避免全局变量的冲突问题
2）封装
3)如果想让外面使用闭包中封装好的方法，或者使用闭包中的其他东西，可以写成window.xx='';
8.上级作用域：被调用的函数，所对应的堆内存在哪个空间下开辟，哪个空间就是上级作用域；







