1.定时器的小技巧：
1）开启定时器前，先关闭没用的定时器；
2）定时器设置的时间不是越小越好，而是越小越不准确；
3）边界值判断；
4)如果不想设置全局变量timer；可以把timer赋值在元素的自定义属性上；oDiv.timer=xxx;
5)如果匿名函数中的东西被占用，就会形成无数个不销毁的私有作用域；非常耗性能；
解决措施：利用_move();解决了参数target的问题；
................................
作业：
1）运动库move3